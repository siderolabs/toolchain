name: bootstrap-c-toolchain
variant: scratch
dependencies:
  - image: "{{ .stagex_prefix }}/core-busybox:{{ .stagex_version }}@sha256:{{ .stagex_busybox_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-gcc:{{ .stagex_version }}@sha256:{{ .stagex_gcc_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-binutils:{{ .stagex_version }}@sha256:{{ .stagex_binutils_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-make:{{ .stagex_version }}@sha256:{{ .stagex_make_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-musl:{{ .stagex_version }}@sha256:{{ .stagex_musl_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-mpfr:{{ .stagex_version }}@sha256:{{ .stagex_mpfr_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-mpc:{{ .stagex_version }}@sha256:{{ .stagex_mpc_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-isl:{{ .stagex_version }}@sha256:{{ .stagex_isl_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-gmp:{{ .stagex_version }}@sha256:{{ .stagex_gmp_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-zlib:{{ .stagex_version }}@sha256:{{ .stagex_zlib_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-libzstd:{{ .stagex_version }}@sha256:{{ .stagex_libzstd_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-linux-headers:{{ .stagex_version }}@sha256:{{ .stagex_linux_headers_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-flex:{{ .stagex_version }}@sha256:{{ .stagex_flex_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-bison:{{ .stagex_version }}@sha256:{{ .stagex_bison_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-m4:{{ .stagex_version }}@sha256:{{ .stagex_m4_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-pkgconf:{{ .stagex_version }}@sha256:{{ .stagex_pkgconf_sha256 }}"
  - image: "{{ .stagex_prefix }}/core-gawk:{{ .stagex_version }}@sha256:{{ .stagex_gawk_sha256 }}"
finalize:
  - from: /
    to: /
